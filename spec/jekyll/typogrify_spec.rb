require 'spec_helper'

describe Jekyll::Typogrify do
  it 'has a version number' do
    expect(Jekyll::Typogrify::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
